import LocalContext from '../../../engine/localContext';

export default new LocalContext({
        name: 'slot-selection',
        ruleFile: 'index.nools',
        agenda: ['slot-selection'],
        dirname: __dirname
});